/**
 * Switches for features that are built but not in play yet. Flip one to true
 * and it's live — nothing else needs changing.
 */
export const flags = {
  /**
   * The "Push further" button on Polihole cards, which reveals a claim's
   * `followUp` line. Off for now; the follow-ups stay in the data.
   */
  pushFurther: false,
};
